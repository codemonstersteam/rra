# rra/docs/features/contracts-graph/plan.md

<!-- Кросс-сервисная фича. Фаза 3 (Plan) + Фаза 5 (Синхронизация) + Фаза 6 (Закрытие). -->

## Фича: contracts-graph
Статус: `todo`
Spec: [`./spec.md`](./spec.md)

## Декомпозиция по сервисам
Порядок строк = **порядок зависимости**. Формат-спека — общий предшественник
(строка 1), оба потребителя зависят от неё.

| # | Сервис (репо)     | Что делает в рамках фичи                                   | Backlog-задача                  | Зависит от | Статус |
|---|-------------------|-----------------------------------------------------------|---------------------------------|------------|--------|
| 1 | `rra-audit-repo`  | зафиксировать формат-спеку `contracts-graph.md` + парсер/валидатор | `rra-audit-repo/backlog.md#<task>` | —          | todo   |
| 2 | `rra-audit-repo`  | проход C: сверка код ↔ дока по формату, класс «противоречие в спецификации» | `rra-audit-repo/backlog.md#<task>` | 1          | todo   |
| 3 | `rra-docs`        | гейт: сверка дока ↔ дока по тому же формату/валидатору     | `rra-docs/backlog.md#<task>`    | 1          | todo   |

## Конвенции связывания
- Единое имя ветки во всех репо: `feat/contracts-graph`.
- PR открываются в порядке зависимости (формат → потребители); в теле PR — `Depends on <repo>#<PR>`.
- Задача в `backlog.md` каждого сервиса ссылается **обратно** на
  `rra/docs/features/contracts-graph/`.

## Синхронизация статусов
Колонка «Статус» обновляется при каждом merge в сервисе. Прогресс не дублируется нигде, кроме этой таблицы.

## Закрытие
Фича → `done`, когда:
- все строки таблицы в статусе `done`, И
- общий `contracts-graph.md` одинаково парсится обоими инструментами и даёт единый
  класс «противоречие в спецификации» (контрактный тест формата зелёный с обеих сторон).
